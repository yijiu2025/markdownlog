```
let lastBackTime = 0; 
ui.emitter.on("back_pressed", (e) => { let currentTime = new Date().getTime(); if (currentTime - lastBackTime < 2000) { // 可在此处添加 exit() 实现退出 } else { toast("再按一次退出"); lastBackTime = currentTime; e.consumed = true; } });
```